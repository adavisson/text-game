import React from 'react';
import ReactTerminal from 'react-terminal-component';

const Term = (props) => {
  return (
    <div id="terminal">
      <ReactTerminal />
    </div>
  );
}
 
export default Term;